%define_enum_with_values(Tile,
    OpenLair, $FD, ; opene means, enemys will spawn out of this one
    ClearedLair, $FE, ; cleared means, that all the monsters has been defeated
    SealedLair, $FF) ; sealed means it's cleared and the player stepped on it
